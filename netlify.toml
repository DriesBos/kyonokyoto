[build]
  base = "."
  command = "npm ci --prefix apps/web && npm --prefix apps/web run build"
  publish = "apps/web/dist"

[functions]
  directory = "apps/web/.netlify/v1/functions"

[build.environment]
  NODE_VERSION = "22.22.0"
